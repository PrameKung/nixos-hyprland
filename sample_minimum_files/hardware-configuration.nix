# Placeholder only. Do not use this empty file to install or boot a machine.
# Generate the real file on the target computer with:
#
#   nixos-generate-config --root /mnt
#
# For a rebuild of an existing system, preserve and reuse that system's current
# /etc/nixos/hardware-configuration.nix instead of copying this placeholder.
{ ... }:

{
}
