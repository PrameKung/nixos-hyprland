# Placeholder only. During installation, run:
#
#   nixos-generate-config --root /mnt
#
# That command must replace this file with the target machine's generated
# filesystem, initrd, swap, platform, and CPU configuration. Never copy UUIDs
# from another computer.
{ ... }:

{
}
